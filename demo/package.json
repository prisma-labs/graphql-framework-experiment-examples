{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node node_modules/.build/start",
    "clean": "rm -rf dist",
    "style": "prettier --write 'src/**/*'",
    "build": "yarn -s clean && pumpkins build",
    "dev": "pumpkins dev",
    "data:seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "pumpkins": "0.0.0-master.b3b2512"
  },
  "devDependencies": {
    "@types/faker": "^4.1.8",
    "faker": "^4.1.0",
    "prettier": "^1.18.2"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  }
}

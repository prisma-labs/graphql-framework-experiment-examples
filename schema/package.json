{
  "name": "hello-santa",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "pumpkins": "../../graphql-santa"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -rf dist",
    "start": "node node_modules/.build",
    "build": "santa build",
    "generate": "santa generate",
    "dev": "santa dev",
    "doctor": "santa doctor",
    "postinstall": "yarn -s link graphql-santa && chmod +x node_modules/.bin/santa"
  },
  "prettier": {
    "semi": false
  }
}

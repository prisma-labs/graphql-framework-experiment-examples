{
  "name": "sans-prisma",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "nexus-future": "../../../nexus/nexus-future"
  },
  "devDependencies": {
    "@types/jest": "^24.9.0",
    "jest": "^24.9.0",
    "prettier": "^1.19.1",
    "ts-jest": "^24.3.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "start": "node node_modules/.build",
    "build": "nexus-future build",
    "generate": "nexus-future generate",
    "dev": "nexus-future dev",
    "doctor": "nexus-future doctor",
    "postinstall": "yarn -s link nexus-future && chmod +x node_modules/.bin/nexus-future"
  },
  "prettier": {
    "semi": false
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}

{
  "name": "sans-prisma",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "nexus": "0.20.0-next.47"
  },
  "devDependencies": {
    "@types/jest": "25.2.2",
    "jest": "26.0.1",
    "prettier": "2.0.5",
    "ts-jest": "26.0.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "start": "node node_modules/.build",
    "build": "nexus build",
    "generate": "nexus generate",
    "dev": "nexus dev",
    "test": "jest"
  },
  "prettier": {
    "semi": false
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}

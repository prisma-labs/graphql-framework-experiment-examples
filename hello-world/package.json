{
  "name": "hello-world",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "graphql-nexus/examples",
  "dependencies": {
    "genql-runtime": "^1.1.21",
    "nexus": "^0.22.0"
  },
  "devDependencies": {
    "@types/jest": "25.2.2",
    "genql-cli": "^1.1.21",
    "jest": "26.0.1",
    "prettier": "2.0.5",
    "ts-jest": "26.0.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "start": "node .nexus/build/api",
    "build": "nexus build",
    "generate": "nexus generate",
    "dev": "nexus dev",
    "test": "jest",
    "generate:client": "genql --schema api.graphql --output client"
  },
  "prettier": {
    "semi": false
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "keywords": [],
  "description": "Example"
}
